<phone:PhoneApplicationPage
    x:Class="helloworld.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
            <Rectangle Fill="Bisque" Height="200" Width="200"/>
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <Rectangle Fill="Azure" Height="100" Width="100"/>
                    <StackPanel Orientation="Vertical">
                        <Rectangle Fill="RosyBrown" Width="100" Height="50"/>
                        <Rectangle Fill="DarkCyan" Width="100" Height="50" />
                    </StackPanel>
                </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Rectangle Fill="Tomato" Height="100" Width="100"/>
                    <StackPanel Orientation="Vertical">
                        <Rectangle Fill="BurlyWood" Width="100" Height="50"/>
                        <Rectangle Fill="SaddleBrown" Width="100" Height="50"/>
                    </StackPanel>
                    </StackPanel>
        </StackPanel>
        </StackPanel>
        <!--
        <Rectangle Fill="Blue" Height="100" Width="100" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Rectangle Fill="Red" Height="100" Width="100" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        <Rectangle Fill="Green" Height="100" Width="100" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Rectangle Fill="Yellow" Height="100" Width="100" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="100,100"/>
        <Rectangle Fill="White" Height="100" Width="100" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="50,0,0,50"/>
        -->
        
        <!--
        <TextBlock FontSize="48">1</TextBlock>
        <TextBlock FontSize="48" Grid.Column="1">2</TextBlock>
        <TextBlock FontSize="48" Grid.Column="2">3</TextBlock>

        <TextBlock FontSize="48" Grid.Row="1">4</TextBlock>
        <TextBlock FontSize="48" Grid.Row="1" Grid.Column="1">5</TextBlock>
        <TextBlock FontSize="48" Grid.Row="1" Grid.Column="2">6</TextBlock>

        <TextBlock FontSize="48" Grid.Row="2">7</TextBlock>
        <TextBlock FontSize="48" Grid.Row="2" Grid.Column="1">8</TextBlock>
        <TextBlock FontSize="48" Grid.Row="2" Grid.Column="2">9</TextBlock>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        -->
        
        <!--
        //grid definition
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*" />
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        
        <Rectangle Fill="Red" Grid.Row="0"/>
        <Rectangle Fill ="Blue" Grid.Row="1"/>
        <Rectangle Fill="Green" Grid.Row="2"/>
           
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

         LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->
        <!--
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="MY APPLICATION" Style="{StaticResource PhoneTextNormalStyle}" Margin="12,0"/>
        </StackPanel>
        -->

        <!--ContentPanel - place additional content here-->
       <!--
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <Button Content="ClickMe" HorizontalAlignment="Left" Height="85" Margin="34,68,0,0" VerticalAlignment="Top" Width="223" Click="Button_Click"/>
            <TextBlock x:Name="resultTextBlock" HorizontalAlignment="Left" Margin="68,174,0,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Width="314" Height="85" FontSize="48"/>

        </Grid>
       -->

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

</phone:PhoneApplicationPage>